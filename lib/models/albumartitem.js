/**
 * Created by tchasid on 1/1/16.
 */
var albumArtItem = function(artistName, albumName, albumArtUrl) {
    this.artistName = artistName;
    this.albumName = albumName;
    this.albumArtUrl = albumArtUrl;
};

module.exports = albumArtItem;